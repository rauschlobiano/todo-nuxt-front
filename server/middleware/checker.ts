
export default defineEventHandler(async (event) => {
   // const id = getRouterParam(event, 'id')
 
   // Do something with id
 
   // return `Checker Middleware is hit`
  //  console.log('From Middleware')
 })